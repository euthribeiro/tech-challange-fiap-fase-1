FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src

# Copiar todos os arquivos
COPY . .

# Restaurar dependências e buildar (testes incluídos)
RUN dotnet restore

# Comando padrão para rodar todos os testes na solution
ENTRYPOINT ["dotnet", "test", "--logger:trx"]
